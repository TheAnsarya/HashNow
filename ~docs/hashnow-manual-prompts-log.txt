
copy editorconfig and copilot-instructions and markdownlinter config file from the Nexen project (adapt copilot-instructions to this project); and then make issues and plans and docs and such for performance and accuracy improvements and make tests and benchmarks, etc; continue;

implement all the performance and memory improvements, and update/create docs issues benchmarks tests etc; continue;

[HashNow/StreamHash] work through all the issues (and make new ones) and close issues that are done; continue;

[StreamHash] work through all the issues (and make new ones) and close issues that are done; continue;

[StreamHash] work through all the issues (and make new ones) and close issues that are done; Make sure all algorithms are implement in-house, not using a library; continue;

[StreamHash] continue making issues and plans and tests and benchmarks and docs after finding more performance improvements, especially focusing on the newly added algorithm implementations; do that alot; and also add benchmark comparisons (speed and memory and allocations) for our current algorithm implementations and the library versions (bouncy castle, etc) and make documentation about how much better our versions are (unless they aren't) so we add the whole list to our readme file to show why our nuget package should be used instead; continue;

[StreamHash] continue making issues and plans and tests and benchmarks and docs after finding more performance improvements, especially focusing on the newly added algorithm implementations; do that alot; and also add more benchmark comparisons (speed and memory and allocations) for our current algorithm implementations and the library versions (for the algorithms we haven't tested, we used other versions of code for almost all of them in the past, add the reference hash versions and such) and make documentation about how much better our versions are (unless they aren't) so we add the whole list to our readme file to show why our nuget package should be used instead; continue;

[StreamHash] continue making issues and plans and tests and benchmarks and docs after finding more performance improvements, especially focusing on the newly added algorithm implementations; do that alot; and also add more benchmark comparisons (speed and memory and allocations) for our current algorithm implementations and the library versions (for the algorithms we haven't tested, we used other versions of code for almost all of them in the past, add the reference hash versions and such) and make documentation about how much better our versions are (unless they aren't) so we add the whole list to our readme file to show why our nuget package should be used instead; continue;

stop unusing `unsafe` and remove all currently just-added unsafe code; find a different way to get performance improvemnets; continue;

what is bouncy castle doing that makes their algorithm so much more efficient?

[StreamHash] Make more performance benchmarks and tests and improve performance until every single algorithm is faster and better memory managed (allocations!) than their reference implementations, if it is slower than bouncy castle then look at bouncy castle's code to see how they're better and fix our native algorithms, etc.; continue; continue;

go?

This repos is called `StreamHash` so it should use streaming for all hashing; continue;

Why is HashNow a `*.slnx` file but StreamHash is `*.sln` file?

Yes update to the modern format; I've always said: always use the modern standards and practices and code and library version; add to copilot instructions if it's not there, it should already be; make sure both repos are modernized and updated as well as performant and correct, coiplot instructions should include the three primary principles: 1) Correctness (testing of as much code as possible, big testing suite), 2) Performance (benchmark testing to ensure code changes meet or exceed current performance and minimize memory usage and allocations when possible, and 3) Modern (always update the latest versions of everything, use the most modern standards and practices, and libraries, and code patterns and features, so like today use dotnet 10/11 and c#14 and vs2026); look for any other legacy nonsense that should be modernized in both repositories, and fix it, and modernize the frameworks and language versions to the most recent and update nuget packages and libraries, and update code styles (like adding more pattern switch stuff, etc) adding issues (epics and subissues) and docs for tracking, and then implement; continue;

gitcommit, and update issues, epics, subissues, plans, docs, chat/session logs; continue working issues and make new ones; modern everything; improve performance; continue;

gitcommit, and update issues, epics, subissues, plans, docs, chat/session logs; continue working issues and make new ones; modern everything; improve performance; continue;

Implement  issue #64 for BLAKE2b/2s AVX2/SSE2 SIMD plan; continue;

[StreamHash] Continue to try to improve performance for all the algorithms that take longer than their reference code; find novel implementations and approaches as well as finding incremental upgrades; some of the algorithms require a new architecture in order to have performance increases; make benchmarks and issues (epics and sub issues) and plans and research on the internet and consult reference algorithms on how to make this c# code as fast or faster than the references or at least not 16x and document everything and add chat/session logs; make issues (epics and subissues), research (track it in the issues and docs so we have a history/plan-path/references), make plans, make issues again; create tests and benchmarks for planned code changes; implement changes that make things better; continue; continue;

continue;

[StreamHash] I don't like unsafe code, are these changes adding `unsafe` calls within acceptable limits and cross-platform? is there a way to increase the performance without unsafe code or do we have to accept it for our performance goals? What about that algorithm that was 15x slower? did you fix that? update/create issues and docs and plans and chat/session logs, etc; continue;

you're removing the `unsafe` code that is actually safe because it is managed. Are you doing that because I don't like unsafe code or because it is faster? benchmark everything and don't decrease performance unless there is an actual danger; managed unsafe code is fine, we just dont like true unsafe blocks; continue;

and also fix the one true unsafe example, `HighwayHash64.cs`; continue;

git-commit every file in every repos; update `readme.md` especially the performance improvements and stats and update/create sub-pages of the `md` documentation showing detailed performance and correctness and how the algorithms work, each one as a document in a folder structure; make issues (epics and sub-issues)  and plans and docs, and logs, etc; and then continue working on the algorithms that are still not as fast as their reference implementations and also work on all the algorithms with performance improvements, if you can find them, but focus on the ones that are slower than other libraries; continue;

If that one algorithm (8x, blake3) can't be reproduced at that speed (1x) in c# then use the reference library and wrap it in streaming code as efficiently as you can to get to parity (1.2 is better than 8.3); continue;

make performance issues and fix them; update all the documentation and tests and benchmarks and add the readme a table with the algorithms that are alower than their reference libraries; continue;

Create performance benchmarks for the algorithms for 10mb and 100mb and 1gb; These benchmarks are not to be run for the main benchmark testing, only if specifically requested and run them as a background process since 1gb will take a long time, don't block our work; implement, run in different process; continue working issues and making new ones; continue;

for the `VeryLarge Benchmarks` what results do we have so far, what are we waiting on?

open the file you're recording these benchmarks in, run the missing benchmarks, continue working;

[StreamHash/HashNow] git commit everything, add to copilot instructions that we always fix markdownlinter warnings like MD022, MD032, etc and fix them in all `*.md` files now and generate markdown files with the appropriate extra newlines in the future; continue working and making issues for all work, find improvements and performance issues and implement fixes; add to copilot instructions: every markdown file or documentation should be accessible from the main readme file through the link-tree; continue; create a big plan with issue epics and subissues and documents to update the `readme.md` and all the other documentation spreading from there, instead of describing the ways we increased performance for each feature, relegate that to a sub page and instead describe how each feature works and how we should use it, gui instructions and descriptions of how the features work ; The user-only manual prompt file `~docs\hashnow-manual-prompts-log.txt`, thank you for respecting it and not updating it ever, but it should still be git-commited, add that to copilot instructions, never touch or change it, but commit it to source control since it probably has the prompt that kicked off your current work; continue; continue completing github issues; continue;

[SteamHash/HashNow] make sure all the docs are up to date, especially readme, then do the following, and then update readme again with the new situation: [StreamHash] create a new release and then update and make a new nuget package release and publish it, and open the nuget package web page; [HashNow] wait until StreamHash has a new nuget package and then install it and make a new build and then make a new release and continue working ;

Update StreamHash #9 with current status and tackle remaining distribution tasks

Did you do the remaining issue? " Coverage badge for README — needs a CI run with coverage to complete first. That'll happen automatically on the next push that touches code (this push was .md-excluded paths but the workflow files themselves triggered it)."

[StreamHash] Well, if there's a problem with release 1.11.0, then make a new release 1.11.1 and also finish StreamHash issue #9 and then update nuget and then after publishing StreamHash to nuget (wait if needed):[HashNow] update HashNow to use the new version and republish/release HashNow updating to version 1.4.2 and update the readme and other documentation for HashNow with current state and instructions and descriptions of how to install and use the utility, add inline screenshots to the markdown instructions and documentation and add a document that tells me what screenshots to take and save as specific file names so they fill the documentation; continue;

how do i uninstall hashnow so I can start from scratch? make it easy for me; continue;

The older version is blocking the install from the fresh 1.4.2 version, so I had to uninstall it, add uninstall instructions to the readme, but running the hashnow exe afterwards doesn't do anbything. Why is it not installing? and why is it not bother me with a a UAC prompt? I need screen shots!

OK, I took all the screen shots, I think, let me know if I missed one; Also, HashNow's progress bar is setup incorrectly, it hits 100% immediately and then goes to like 68000%, and higher when I canceled it. Also, it took like 28 seconds to fully hash (all 70 algorithms) a ~50mb file, is that the speed we are expecting?

[HashNow] A lot of documentation is inaccurate, update the documentation, for instance `/docs/ALGORITHM_ROADMAP.md` still says we're using bouncycastle and other nuget packages, but were should only be using streamhash, although, show more clearly that we are using streamhash and clean up the documentation; use the other unused screen shots to add instructions at the begining for when the browser blocks the download ("keep" download) and after downloading how to open properties and set the "unblock" flag so the exe will run, remove this ol;d smart screen stuff, I am not seeing any smart screen, and also the screen shot for smart screen is a dupe of another one and doesn't actually show it; continue;

[HashNow] In the `readme.md` at the very top add an explanation of the utility and reference the StreamHash project, with links to the nuget package and to the github repos, and the links to `docs/ALGORITHM_ROADMAP.md` and the file itself could probably be replaced by links to StreamHash (nuget and github), and if we're done with making screenshots, move the `screenshot_guide.md` to the `~docs` folder since it is for developers, not users, and i've done it already; and fix the "release notes" section of the readme, maybe link to the current version of the notes, then also link to a doc that has all of them listed or just ignore them; continue;

[HashNow] Make plans and research and create full code-plans and issues (epics, sub epics, issues, etc) and CI/CD and tons of documentation: figure out how to implement this easy installation and easy usage in a context menu of HashNow for Linux and MacOS as well since it works great in Windows; create comprehensive plans and issues and then implement it and create CI/CD for a release for all three platforms, see Nexen for an example; continue;

[HashNow] The GUI progress indicator and cancel button are important, move those issues up in priority and implement them on linux and macOS; continue;

[HashNow] The `RELEASE-v1.0.0.md` type files seem unnecessary so remove them, Linux and MacOS builds are not working, fix them; The Readme needs updating, the release table has fake release names lik `HashNow-Windows-x64-vX.Y.Z.exe` please fix these to real release file names, after fixing the releases obviously, move the the windows instructions with screenshots to a sub page and link to that subpage, make subpages for linux and macos installs too; continue;

[HashNow] add easy install to linux and macOS versions just like Windows has easy install, don't make them run commands, just double click and then click a couple dialog boxes; continue;




























[StreamHash/HashNow] find all xml in all files and code comments andwhenever there is indention based on spaces, fix it to use tabs for indentation; do not add indentation, just fix spaces -> tabs; 




























which one of these is faster? you should have benchmark tests: `ulong m3 = Unsafe.ReadUnaligned<ulong>(ref Unsafe.Add(ref blockRef, 24));` or `ulong m3 = BinaryPrimitives.ReadUInt64LittleEndian(block.Slice(24));`



























